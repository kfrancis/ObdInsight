<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ObdInsight.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:ObdInsight.ViewModels"
    Title="{Binding Title}"
    x:DataType="vm:SettingsViewModel">

    <ScrollView>
        <VerticalStackLayout Padding="{StaticResource AppPagePadding}" Spacing="{StaticResource AppSpacingLg}">

            <Label Style="{StaticResource AppTextHero}" Text="Settings" />

            <!--  App Settings  -->
            <Border StrokeShape="RoundRectangle 20" Style="{StaticResource AppCard}">
                <VerticalStackLayout Spacing="{StaticResource AppSpacingMd}">
                    <Label Style="{StaticResource AppTextTitle}" Text="Application" />

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Units" VerticalOptions="Center" />
                        <Picker
                            Grid.Column="1"
                            BackgroundColor="#CC45D1C5"
                            ItemsSource="{Binding AvailableUnits}"
                            SelectedItem="{Binding SelectedUnit}"
                            WidthRequest="120" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!--  Vehicle Settings  -->
            <Border StrokeShape="RoundRectangle 20" Style="{StaticResource AppCard}">
                <VerticalStackLayout Spacing="{StaticResource AppSpacingMd}">
                    <Label Style="{StaticResource AppTextTitle}" Text="Vehicle" />

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Active Vehicle" VerticalOptions="Center" />
                        <Label
                            Grid.Column="1"
                            Text="{Binding ActiveVehicleName}"
                            TextColor="{AppThemeBinding Light={StaticResource AppTextSecondaryLight},
                                                        Dark={StaticResource AppTextSecondaryDark}}"
                            VerticalOptions="Center" />
                    </Grid>

                    <Button
                        BackgroundColor="#45D1C5"
                        Command="{Binding ManageCarProfileCommand}"
                        Text="Configure Car Profile" />
                </VerticalStackLayout>
            </Border>

            <!--  About  -->
            <Border StrokeShape="RoundRectangle 20" Style="{StaticResource AppCard}">
                <VerticalStackLayout Spacing="{StaticResource AppSpacingMd}">
                    <Label Style="{StaticResource AppTextTitle}" Text="About" />

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Version" VerticalOptions="Center" />
                        <Label
                            Grid.Column="1"
                            Text="{Binding AppVersion}"
                            TextColor="{AppThemeBinding Light={StaticResource AppTextSecondaryLight},
                                                        Dark={StaticResource AppTextSecondaryDark}}"
                            VerticalOptions="Center" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource AppOutlineLight}, Dark={StaticResource AppOutlineDark}}" />

                    <Button
                        BackgroundColor="#45D1C5"
                        Command="{Binding ViewLicensesCommand}"
                        Text="View Licenses" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
